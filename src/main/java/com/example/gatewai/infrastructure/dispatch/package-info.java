/**
 * Carbon-aware deferred dispatch adapter (Phase 4.4): in-memory job store and
 * the scheduled worker that routes queued jobs to the greenest zone.
 */
package com.example.gatewai.infrastructure.dispatch;
